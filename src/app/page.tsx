import type {Metadata} from "next";

export const metadata: Metadata = {
    title: "Items list",
    description: "Page with list of items",
};

const Home = () => (
    <div>Home</div>
)

export default Home